# Залежності (вони займають багато місця і легко відновлюються командою npm install)
node_modules/ [cite: 218]

# Системні файли macOS
.DS_Store [cite: 219, 236]

# Логи та тимчасові файли
*.log [cite: 219]
npm-debug.log*

# Секрети та конфігурації середовища
.env [cite: 220, 243]

# Артефакти збірки (якщо в майбутньому додаси бандлер)
dist/ [cite: 221]
build/ [cite: 221]

# Звіти про тестування (генеруються локально, не потрібні в репозиторії)
coverage/
playwright-report/
test-results/

# IDE та редактори (щоб конфігурації твого VS Code не заважали іншим)
.vscode/
.idea/

# Тимчасові файли Vite та кеш лінтера
.vite/
.eslintcache

# Локальні змінні оточення (додатково до .env)
.env.local
.env.*.local

# Тимчасові файли TypeScript (якщо Vite їх згенерує)
*.tsbuildinfo